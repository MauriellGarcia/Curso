<p id="notice">
  <%= notice %>
</p>
<main>

  <section class="py-1 text-center container">
    <div class="row py-lg-1">
      <div class="col-lg-6 col-md-8 mx-auto">
        <h1 class="fw-light">Libros Disponibles</h1>
        <p class="lead text-muted">Disfrute de todos nuestros libros.</p>
        <p>
        </p>
      </div>
    </div>
  </section>

  <div class="album py-5 bg-light">
    <div class="container">
      <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
        <% @books.each do |books| %>
          <div class="col">
            <div class="card shadow-sm border-primary">
                    <div class="card-body">
                  <p class="card-text">
                  <h3>
                    <%= books.title %>
                  </h3>
                  <%= sanitize(books.category) %>
                    </p>
                    <div class="d-flex justify-content-between align-items-center">
                      <div class="btn-group">
                      </div>
                      <small class="text-muted">
                        <%= sprintf("%d", books.year) %>
                      </small>
                    </div>
                </div>
            </div>
          </div>
          <% end %>
      </div>
    </div>
  </div>
</main>